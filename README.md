A simple Dockerized template for a Flask app served by uWSGI and Nginx.

Prerequisites:
  - Docker
  - Docker Compose

To build & run:

    docker-compose build
    docker-compose up

Then the website is available at http://0.0.0.0
